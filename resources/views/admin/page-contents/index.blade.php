@extends('admin.layout')
@section('title', 'Konten Halaman')
@section('content')
    <div class="mb-6 flex items-center justify-between"><p class="text-slate-600">Edit teks hero, section, dan konten tiap halaman.</p><a href="{{ route('admin.page-contents.create') }}" class="rounded-xl bg-primary px-5 py-3 text-sm font-bold text-white">Tambah Konten</a></div>
    <div class="overflow-hidden rounded-3xl bg-white shadow-sm"><table class="w-full text-left text-sm"><thead class="bg-slate-50 text-xs uppercase tracking-[0.12em] text-slate-500"><tr><th class="p-4">Page</th><th>Section</th><th>Field</th><th>Value</th><th class="text-right pr-4">Aksi</th></tr></thead><tbody class="divide-y">@foreach ($contents as $content)<tr><td class="p-4 font-bold text-primary">{{ $content->page }}</td><td>{{ $content->section }}</td><td>{{ $content->field }}</td><td class="max-w-md truncate text-slate-500">@if ($content->type === 'image' && $content->value)<div class="flex items-center gap-3"><img src="{{ $content->value }}" alt="{{ $content->field }}" class="h-12 w-12 rounded-xl object-cover"><span class="truncate">{{ $content->value }}</span></div>@else{{ $content->value }}@endif</td><td class="pr-4 text-right"><a href="{{ route('admin.page-contents.edit', $content) }}" class="font-bold text-primary">Edit</a><form method="POST" action="{{ route('admin.page-contents.destroy', $content) }}" class="inline" onsubmit="return confirm('Hapus konten ini?')">@csrf @method('DELETE')<button class="ml-3 font-bold text-red-600">Hapus</button></form></td></tr>@endforeach</tbody></table></div><div class="mt-6">{{ $contents->links() }}</div>
@endsection
