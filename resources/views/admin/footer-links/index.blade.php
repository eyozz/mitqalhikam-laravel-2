@extends('admin.layout')
@section('title', 'Footer Links')
@section('content')
    <div class="mb-6 flex items-center justify-between"><p class="text-slate-600">Kelola social media dan navigasi footer. Icon tersedia: Instagram, Facebook, TikTok, YouTube.</p><a href="{{ route('admin.footer-links.create') }}" class="rounded-xl bg-primary px-5 py-3 text-sm font-bold text-white">Tambah Link</a></div>
    <div class="overflow-hidden rounded-3xl bg-white shadow-sm"><table class="w-full text-left text-sm"><thead class="bg-slate-50 text-xs uppercase tracking-[0.12em] text-slate-500"><tr><th class="p-4">Label</th><th>URL</th><th>Group</th><th>Icon</th><th>Status</th><th class="text-right pr-4">Aksi</th></tr></thead><tbody class="divide-y">@foreach ($links as $link)<tr><td class="p-4 font-bold text-primary">{{ $link->label }}</td><td class="max-w-md truncate">{{ $link->url }}</td><td>{{ $link->group }}</td><td>{{ $link->icon ?: '-' }}</td><td>{{ $link->is_active ? 'Aktif' : 'Nonaktif' }}</td><td class="pr-4 text-right"><a href="{{ route('admin.footer-links.edit', $link) }}" class="font-bold text-primary">Edit</a><form method="POST" action="{{ route('admin.footer-links.destroy', $link) }}" class="inline" onsubmit="return confirm('Hapus link ini?')">@csrf @method('DELETE')<button class="ml-3 font-bold text-red-600">Hapus</button></form></td></tr>@endforeach</tbody></table></div><div class="mt-6">{{ $links->links() }}</div>
@endsection
